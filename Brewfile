# Machine brew manifest, captured 2026-08-17 from the firstmate repo root
# after a capture-workflow miss. configuration.nix's brews/casks arrays
# (with onActivation.cleanup = "uninstall") remain the enforcing source of
# truth; nothing here runs `brew bundle` automatically - update-all.sh uses
# `brew update && brew upgrade`.
tap "kunchenguid/tap", trusted: true
# GitHub command-line tool
brew "gh"
# Agent multiplexer that lives in your terminal
brew "herdr"
# CLI for NextDNS's DNS-over-HTTPS (DoH)
brew "nextdns"
# Open-source, cross-platform JavaScript runtime environment
brew "node"
# Terminal multiplexer
brew "tmux"
# Stand alone ad blocker
cask "adguard"
# Menu-bar app that writes its own widgets
cask "kunchenguid/tap/baby-menu"
# Messaging app with a focus on speed and security
cask "telegram"
# GPU-accelerated cross-platform terminal emulator and multiplexer
cask "wezterm"
# Native desktop client for WhatsApp
cask "whatsapp"
